# AGENTS.md

本文件是 GobulinGame 项目的协作规则。目标是让每次改动都延续当前架构，并逐步把项目从 First Person 模板和早期 Demo 收敛为可扩展的正式实现。

## 项目事实来源

判断项目当前行为时，按以下优先级读取：

1. 已在编辑器中确认的资产和当前工作区源码。
2. [docs/README.md](docs/README.md) 中的当前文档索引。
3. [docs/03-技术设计文档.md](docs/03-技术设计文档.md)、[docs/05-开发路线图.md](docs/05-开发路线图.md) 和 [docs/06-设计决策记录.md](docs/06-设计决策记录.md)。
4. [.agents/ue-project-context.md](.agents/ue-project-context.md) 中的工程上下文。
5. `docs/future/` 只表示后续方案，`docs/archive/` 只用于历史追溯，不能覆盖当前代码事实。

当前项目为 Unreal Engine 5.8 单机基础战斗垂直切片，Runtime 模块为 `GobulinGame`，Editor 模块为 `GobulinGameEditor`。当前重点是正式玩家、第一把剑和正式敌人的基础闭环；AI、Mass 敌潮、Perk、基地和联机属于后续阶段。

## 开始任务前

1. 先查看 `git status` 和相关 diff，区分用户已有改动、历史残留和本次任务范围。
2. 判断任务属于玩家、战斗、武器、敌人、数据、编辑器工具或其他领域。
3. 读取对应文档和源码，不根据旧模板或过时文档猜测当前实现。
4. 对会影响架构、资产语义、规则口径、蓝图流程或验收结果的歧义，先说明假设并向用户确认。
5. 普通实现细节按照现有代码、设计决策和 Unreal 规范自行判断，不为了细节反复阻塞任务。
6. 多模块或较大重构先说明目标架构、影响范围、迁移/删除的旧路径和验证方式，再分小切片实现。

## 当前架构边界

- C++ 负责运行时规则、状态、数据契约和可复用组件。
- 蓝图负责数据资产配置、组件组合、材质/曲线/贴图引用、表现装配以及用户明确要求的 GameMode/地图配置。
- 不把策划数值、资产路径和表现参数散落在角色或 UI 代码中；优先使用 DataAsset、DataTable、Curve 和 GameplayTags。
- 能用小型组件、子系统或数据结构表达的职责，不全部堆进 Character、Controller 或巨型管理类。
- 公共接口要保持稳定；具体算法和内部状态尽量封装在 `.cpp` 或组件内部。
- 只有需要反射、序列化、蓝图访问、DataAsset、GC 或编辑器遍历时才使用 UObject 反射宏。
- 头文件尽量前向声明，具体依赖放到 `.cpp`；UObject 引用优先使用 `TObjectPtr`。
- 新增 GameplayTag 需要集中声明，并同步相关战斗文档。

当前正式实现包括：

- `AGobulinPlayerCharacter`、`AGobulinPlayerController`。
- `UBattleAttributeComponent` 和 `IDamageable` 伤害入口。
- `UGobulinSwordCombatComponent`、`UGobulinWeaponViewComponent`、`UGobulinSwordFeedbackComponent`。
- `UGobulinSwordDefinition`：第一把剑的数据资产。
- `AGobulinEnemyCharacter`、`UGobulinEnemyDefinition`、`UGobulinEnemyAnimationComponent`。
- 第一人称武器采用相机挂载 Plane + 贴图变换曲线；敌人使用 `USpriteFlipbook` + `USpriteBillboardComponent`。

`ADemoEnemy`、模板玩家类和旧武器体系只作为过渡或历史参考。新增正式功能不要重新依赖它们；如果需要删除或迁移旧路径，先检查引用并说明影响。

## Unreal 实现规则

- 遵循 Unreal 命名规范：`A/U/F/E/I` 前缀、PascalCase、布尔属性使用 `b` 前缀。
- 新增类名继续使用 `Gobulin` 前缀，避免引入含义模糊或仅服务临时 Demo 的命名。
- 新增编辑器可见属性、DataAsset 字段和调试参数使用中文 `ToolTip` 或代码注释，说明用途、单位和推荐调参范围。
- 不用过度严格的 `ClampMin/ClampMax` 代替调参说明；只有真实的运行时安全、引擎 API 或资产合法性约束才设置硬限制。
- 需要时间推进的表现可以使用 Tick，但应限制在实际需要的组件内；能用事件或定时器解决时不要增加无意义的全局 Tick 轮询。
- 运行时系统不得依赖编辑器专用代码或交互式编辑器状态。
- 不直接复制第三方项目的代码、Shader、资产或工程结构；参考资料只用于行为、节奏和视觉目标，实际方案实现为 Gobulin 原生的 UE/C++/蓝图方案。

## 玩家、武器和敌人约定

- 玩家输入使用 Enhanced Input；新增输入 Action 和 Mapping 必须说明归属，并避免恢复已经废弃的 W01/W03/W05 输入体系。
- 当前剑系统由战斗组件推进攻击状态、输入缓存、自动连击、剑尖轨迹命中和伤害派发；表现由 Weapon View 和反馈组件负责。
- 第一人称武器不引入 Flipbook、手部模型或不必要的 3D 武器层，除非用户重新确认设计方向。
- 敌人正式实现以 `AGobulinEnemyCharacter` 为基础，由 Definition 提供数据，由 AnimationComponent 管理纸片动画状态。
- 伤害必须通过统一的 `IDamageable` / BattleAttribute 路径处理，不在武器表现层直接修改敌人生命值。
- 相机震动、Hit Stop、音效等反馈属于表现反馈层，不应反向持有战斗规则。

## 编辑器、蓝图与资产工作

- 不默认修改用户负责的蓝图类、GameMode 或地图配置；只有用户明确要求时才执行，并列出具体资产路径。
- `.uasset` 和 `.umap` 不做文本合并，也不通过随意脚本覆盖无关资产。
- 创建或修改贴图、材质、曲线、DataAsset、蓝图和地图时，明确写出实际变化路径和需要用户在编辑器确认的内容。
- 需要人眼判断的内容必须通过编辑器或 PIE 验收：贴图透明边缘、Plane 比例、曲线节奏、攻击轨迹、动画切换、材质观感、音效时机、相机反馈和输入绑定。
- 未经编辑器确认的表现效果，不在最终说明中声称已经完全验收。
- 代码编译前确保 Unreal Editor 已关闭；编辑器资产验收和 C++ 编译分开进行。

## 调试、修复与重构

- 先复现并定位根因，再决定修改点；不要只在报错行或当前调用点打补丁。
- 如果问题暴露出生命周期、所有权、输入路由、状态同步或模块边界问题，应优先修正公共路径。
- 正式架构允许替换早期模板和 Demo，但大范围删除或迁移前必须列出旧路径、引用范围、迁移策略和验证方式。
- 临时 workaround 必须说明适用范围、可能复发的场景和正式清理方向；必要时同步到设计决策或技术债文档。
- 不静默改变用户已经确认的玩法规则、资产语义、存档结构、蓝图制作流程或 GameMode 配置。

## 编译、测试与验收

- 纯文档、规划或 `AGENTS.md` 修改不需要编译。
- C++ 改动按风险做必要的模块编译；不为了形式额外编写或运行无关测试。
- 项目早期处于玩法和架构快速迭代阶段，不要为了追求测试数量过早制作大量自动化测试。优先保证核心规则能够编译、关键路径能够复现、编辑器表现能够验收；等伤害协议、属性系统、敌人生命周期等接口稳定后，再集中补充高价值的契约测试和回归测试。
- 规则、状态、伤害协议、生命周期和容易回归的边界问题优先做定向验证；纯视觉和手感问题优先交给编辑器/PIE 验收。
- 如果用户明确要求“不做不必要的测试”，只执行必要编译、相关检查和明确的编辑器验收步骤。
- 最终说明必须区分：已完成的源码验证、未运行的测试、需要用户确认的编辑器项目以及剩余风险。

## Git 与文件安全

- 保护用户已有修改，不使用 `git reset --hard`、`git checkout --` 或批量删除来清理工作区。
- 删除、移动或覆盖文件前先确认准确目标和引用范围；能保留历史时优先移动到归档或使用可恢复方式。
- 文件编辑优先使用小范围 patch；不把 `Binaries`、`Intermediate`、缓存或临时生成物作为主要交付内容。
- 未经用户明确要求，不主动创建提交、切换分支、推送远程或修改外部项目。
- 完成后报告实际修改文件、验证结果和仍需用户处理的资产/编辑器步骤。

## 文档同步与完成标准

- 当前实现、验收口径和重要边界同步到 `docs/` 对应文档；长期设计决策追加到 ADR。
- 未来方案放入 `docs/future/`，被替代的历史方案放入 `docs/archive/`，不要让旧文档继续伪装成当前实现。
- 完成标准不是“代码能编译”，而是行为符合当前文档、职责边界清晰、后续可扩展，并且需要的编辑器验收项目已经明确列出。
